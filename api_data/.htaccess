# BLOQUEIO DE SEGURANÇA - PLENA FACTORY
# Impede acesso direto aos arquivos JSON via navegador (HTTP)
# O PHP ainda consegue ler os arquivos via sistema de arquivos local.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>

# Desativa listagem de diretório por precaução
Options -Indexes
